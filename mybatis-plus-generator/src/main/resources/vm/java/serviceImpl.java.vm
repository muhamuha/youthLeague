package ${cfg.packageName}.service.impl;

import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import com.wzxc.common.core.text.Convert;
import com.wzxc.common.utils.DateUtils;
import ${cfg.packageName}.dao.ds1.${cfg.voName}Mapper;
import ${cfg.packageName}.vo.${cfg.voName};
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import ${cfg.packageName}.service.I${cfg.voName}Service;

import java.util.List;

/**
 * <p>
 * 服务实现类 【请填写功能名称】
 * </p>
 *
 * @author ${cfg.author}
 * @date ${cfg.datetime}
 */
@Service
public class ${cfg.voName}ServiceImpl extends ServiceImpl<${cfg.voName}Mapper, ${cfg.voName}> implements I${cfg.voName}Service {

    @Autowired
    private ${cfg.voName}Mapper ${cfg.moudleName}Mapper;

    /**
     * 查询【请填写功能名称】列表
     *
     * @param ${cfg.moudleName} 【请填写功能名称】
     * @return 【请填写功能名称】
     */
    @Override
    public List<${cfg.voName}> select${cfg.voName}List(${cfg.voName} ${cfg.moudleName}) {
        return ${cfg.moudleName}Mapper.select${cfg.voName}List(${cfg.moudleName});
    }

    /**
     * 新增【请填写功能名称】
     *
     * @param ${cfg.moudleName} 【请填写功能名称】
     * @return 结果
     */
    @Override
    public int insert${cfg.voName}(${cfg.voName} ${cfg.moudleName}) {
        return ${cfg.moudleName}Mapper.insert${cfg.voName}(${cfg.moudleName});
    }

    /**
     * 修改【请填写功能名称】
     *
     * @param ${cfg.moudleName} 【请填写功能名称】
     * @return 结果
     */
    @Override
    public int update${cfg.voName}(${cfg.voName} ${cfg.moudleName}) {
        return ${cfg.moudleName}Mapper.update${cfg.voName}(${cfg.moudleName});
    }



}