<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wzxc.busi.dao.LeagueAnswerMapper">
    
    <resultMap type="LeagueAnswer" id="LeagueAnswerResult">
        <result property="id"    column="id"    />
        <result property="answerId"    column="answer_id"    />
        <result property="answerName"    column="answer_name"    />
        <result property="content"    column="content"    />
        <result property="publishId"    column="publish_id"    />
    </resultMap>

    <sql id="selectLeagueAnswerVo">
        select a.id, a.answer_id, a.content, a.publish_id, a.answer_name from league_answer as a
    </sql>

    <select id="selectLeagueAnswerList" parameterType="LeagueAnswer" resultMap="LeagueAnswerResult">
        <include refid="selectLeagueAnswerVo"/>
        <where>
            <if test="answerName != null and answerName != ''"> and a.answer_name = #{answerName}</if>
            <if test="publishId != null "> and a.publish_id = #{publishId}</if>
        </where>
        order by create_time desc
    </select>
        
    <insert id="insertLeagueAnswer" parameterType="LeagueAnswer" useGeneratedKeys="true" keyProperty="id">
        insert into league_answer
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="answerId != null">answer_id,</if>
            <if test="answerName != null and answerName != ''">answer_name,</if>
            <if test="content != null and content != ''">content,</if>
            <if test="publishId != null">publish_id,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="answerId != null">#{answerId},</if>
            <if test="answerName != null and answerName != ''">#{answerName},</if>
            <if test="content != null and content != ''">#{content},</if>
            <if test="publishId != null">#{publishId},</if>
         </trim>
    </insert>

    <update id="updateLeagueAnswer" parameterType="LeagueAnswer">
        update league_answer
        <trim prefix="SET" suffixOverrides=",">
            <if test="id != null">id = #{id},</if>
            <if test="answerId != null">answer_id = #{answerId},</if>
            <if test="content != null and content != ''">content = #{content},</if>
            <if test="publishId != null">publish_id = #{publishId},</if>
        </trim>
        where id = #{id}
    </update>

</mapper>